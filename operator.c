#include <stdio.h>
int main()
{
    // int a = 20;
    // int b = 6;
    // b=b/6;
    // b+=6;
    // printf("%f", (float)a / (float)b);
    // printf("%d\n", 85 > 44);
    // printf("%d", 26 > 44);

    // printf("%d",!(85<44) );
    // printf("%d", 96 & 42);
    // printf("%d", 96>>2);
    // printf("%d", 96<<2);
    // printf("%d", 96 | 42);
    // And(&&)
    // OR(||)
    // not(!)

    return 0;
}

/*

8+9  //expression

operands means the value or the variable on which operator works

- operator

single operand
unary operator

increment or decrement (++  ,  -- )

-------------------------------------------------------
binary operator

type of operators

arithmetic + - * / %

comparison or relational
> < >= <= == !=

logical operator
and(&&) or(||)Piping sign not(!)excalmation

assignement operator
= += -= /= %=

bitwise operator
& | >> <<

miscelleanous operator
conditional or ternary operator
(?:)

pointer *
address &
sizeof()









*/